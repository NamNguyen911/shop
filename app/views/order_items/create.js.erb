<% if @order.errors.any? || @order_item.errors.any? %>
  alert("not valid.")
<% else %>
  // $(".cart-text").html("<%= escape_javascript(render 'layouts/cart_text', layout: false) %>")
  $(".item-in-cart").text("<%= @order.order_items.size %>")
  $("#order-status").text("Đã thêm vào giỏ hàng!")
<% end %>
